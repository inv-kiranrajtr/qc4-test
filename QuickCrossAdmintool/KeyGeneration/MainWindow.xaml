<Window x:Class="KeyGeneration.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeyGeneration"
        mc:Ignorable="d"
        Title="PRO-Activation Key Generation" Height="600" Width="1000" Loaded="Window_Loaded" WindowStyle="None" 
			ResizeMode="NoResize" WindowStartupLocation="CenterScreen" >
    <Window.Resources>
        <!--Button and header styles -->
        <ResourceDictionary>
            <Style x:Key="Header" TargetType="{x:Type Grid}">
                <Setter Property="Background" Value="#3467b0"/>
            </Style>
            <Style x:Key="HeaderText" TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Text" Value="PRO-Activation Key Generation"/>
                <Setter Property="Padding" Value="24,10"/>
                <Setter Property="FontSize" Value="30"/>
                <Setter Property="FontWeight" Value="Normal"/>
            </Style>
            <Style TargetType="Button" x:Key="Button">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0" Padding="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"></ContentPresenter>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="true">
                        <Setter Property="Background" Value="#aeaeae"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"  Style="{StaticResource Header}" MouseDown="DragableGridMouseDown">
            <!-- Window Header -->
            <TextBlock Style="{StaticResource HeaderText}"/>
        </Grid>
        
        <Grid Grid.Row="1" Margin="24,24,24,0">
            <!-- 1st grid with buttons and textboxes-->
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="9*"/>
                <ColumnDefinition Width="36*"/>
                <ColumnDefinition Width="46*"/>
                <ColumnDefinition Width="90*"/>
                <ColumnDefinition Width="91*"/>
                <ColumnDefinition Width="91*"/>
                <ColumnDefinition Width="90*"/>
                <ColumnDefinition Width="136*"/>
                <ColumnDefinition Width="136*"/>
                <ColumnDefinition Width="91*"/>
                <ColumnDefinition Width="136*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Decrypt ID" Click="BtnDecryptKey_Click" Style="{StaticResource Button}" Grid.Column="2" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="74" RenderTransformOrigin="3.614,0.982" Grid.ColumnSpan="2" Margin="31,6"/>
            <Label x:Name="labelKeyType" Content="Key Type" Grid.Column="5" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,8"/>
            <TextBox x:Name="txtKeyType" Grid.Column="6" HorizontalAlignment="Left" Height="22"  TextWrapping="Wrap" Text="PRO" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"  IsReadOnly="True"
                         VerticalAlignment="Center" Width="50" Margin="0,10" BorderThickness="2" GotFocus="TxtKeyType_GotFocus"/>
            <Label x:Name="labelExpDate" Content="Expiration Date" Grid.Column="7" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,8"/>
            <DatePicker x:Name="date_expiry" Grid.Column="8" HorizontalAlignment="Left" Height="22" VerticalAlignment="Center" Width="100" Margin="0,10" Cursor="Hand"/>
            <Button Content="Generate" Click="BtnGenerateKey_Click" Style="{StaticResource Button}" Grid.Column="9" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="75" RenderTransformOrigin="3.614,0.982" Margin="8,6"/>
            <Button Content="Delete" Click="BtnDeleteKey_Click" Style="{StaticResource Button}" Grid.Column="10" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="76" RenderTransformOrigin="3.614,0.982" Margin="30,6"/>

        </Grid>

        <Grid Grid.Row="2" Margin="28" VerticalAlignment="Stretch" Background="Beige">
            <!-- 2nd grid with datagrid and header-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="10*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Background="#cccccc">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{Binding ElementName=gridSystemInfo, Path=RowHeaderWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column1, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column2, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column3, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column4, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column5, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column6, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column7, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column8, Path=ActualWidth}" />
                        <ColumnDefinition Width="{Binding ElementName=Column9, Path=ActualWidth}" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" BorderThickness="1" BorderBrush="White">
                    </Border>
                    <Border Grid.Column="3" Grid.ColumnSpan="4" HorizontalAlignment="Stretch" BorderThickness="1" BorderBrush="White">
                        <Label HorizontalAlignment="Center" FontWeight="Medium">System Information</Label>
                    </Border>
                    <Border Grid.Column="7" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" BorderThickness="1" BorderBrush="White">
                        <Label HorizontalAlignment="Center" FontWeight="Medium">AdditionalInformation</Label>
                    </Border>
                    <Border Grid.Column="9" HorizontalAlignment="Stretch" BorderThickness="1" BorderBrush="White" >
                    </Border>
                </Grid>
                <DataGrid Name="gridSystemInfo" Grid.Row="1" RowHeaderWidth="5" CanUserSortColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserResizeColumns="True"
                      ItemsSource="{Binding}" AutoGenerateColumns="False" PreviewKeyDown="GridSystemInfo_PreviewKeyDown" Margin="0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding [Sl No]}" Header="Sl No" Width="50" x:Name="Column1" />
                        <DataGridTextColumn Binding="{Binding [System ID]}" Header="System ID" Width="120" x:Name="Column2"/>
                        <DataGridTextColumn Binding="{Binding [User domain]}" Header="User domain" Width="100" x:Name="Column3">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                        <CheckBox x:Name="chkGridColumn"  HorizontalAlignment="Center"></CheckBox>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding [User name]}" Header="User name" Width="100" x:Name="Column4">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                        <CheckBox x:Name="chkGridColumn"  HorizontalAlignment="Center"></CheckBox>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding [Computer name]}" Header="Computer name" Width="100" x:Name="Column5">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                        <CheckBox x:Name="chkGridColumn"  HorizontalAlignment="Center"></CheckBox>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding [Mac address]}" Header="Mac address" Width="100" x:Name="Column6">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                        <CheckBox x:Name="chkGridColumn"  HorizontalAlignment="Center"></CheckBox>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding [E mail]}" Header="E mail" Width="100" x:Name="Column7"/>
                        <DataGridTextColumn Binding="{Binding [Note]}" Header="Note" Width="100" x:Name="Column8"/>
                        <DataGridTextColumn Binding="{Binding [Activation Key]}" Header="Activation Key" Width="*" x:Name="Column9"/>
                    </DataGrid.Columns>
                    <DataGrid.Resources>
                        <Style TargetType="DataGrid">
                            <EventSetter Event="MouseUp" Handler="DataGrid_MouseUp"/>
                            <EventSetter Event="MouseDoubleClick" Handler="DataGrid_MouseDoubleClick"/>
                        </Style>
                        <Style TargetType="CheckBox">
                            <EventSetter Event="Click" Handler="CheckBox_Click"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <EventSetter Event="PreviewMouseDown" Handler="DataGridCell_MouseDown"/>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
                </Grid>
            </Grid>

        <Grid Grid.Row="3" Margin="24,0">
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="160*"/>
                <ColumnDefinition Width="30*"/>
                <ColumnDefinition Width="30*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Show Key" Click="BtnShowKey_Click" Style="{StaticResource Button}" Grid.Column="1" HorizontalAlignment="Center"  VerticalAlignment="Top" Width="75" RenderTransformOrigin="3.614,0.982"/>
            <Button Content="Exit" Click="BtnExitKey_Click" Style="{StaticResource Button}" Grid.Column="2" HorizontalAlignment="Center"  VerticalAlignment="Top" Width="75" RenderTransformOrigin="3.614,0.982"/>
        </Grid>
    </Grid>
</Window>



