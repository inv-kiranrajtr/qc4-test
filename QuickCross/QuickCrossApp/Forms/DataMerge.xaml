<Window x:Class="Qc4Launcher.Forms.DataMerge"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Qc4Launcher"
        mc:Ignorable="d"
        Title="{x:Static local:LocalResource.TITLE_DATA_MERGE}"  Height="480" Width="700" ResizeMode="NoResize" WindowStyle="None" 
			WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" Closed="Window_Closed" BorderBrush="#ADADAD" BorderThickness="1">
    <Window.Resources>

        <Style TargetType="Button" x:Key="helpButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"></ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="true">
                    <Setter Property="Background" Value="#aeaeae"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <Style TargetType="Button" x:Key="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"></ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="true">
                    <Setter Property="Background" Value="#3467b0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="HeaderText" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Text" Value="{x:Static local:LocalResource.LBL_DATA_MERGE_TITLE}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <Style x:Key="Header" TargetType="{x:Type Grid}">
            <Setter Property="Background" Value="#3467b0"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="385*"/>
                <RowDefinition Height="75*"/>
            </Grid.RowDefinitions>
            <Grid  Grid.Row="0"  MouseDown="DragableGridMouseDown" Style="{StaticResource Header}">
                <TextBlock Style="{StaticResource HeaderText}"/>
                <Button Name="Button_Help" Width="50" Height="25" Style="{StaticResource helpButton}" Content="{x:Static local:LocalResource.BTN_HELP}" HorizontalAlignment="Right" Margin="0,2,30,0" Click="help_Btn_Click" VerticalAlignment="Top"></Button>
            </Grid>

            <Grid  Grid.Row="1" Margin="24,20" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="303*"/>
                    <ColumnDefinition Width="23*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="212*"/>
                    <RowDefinition Height="165*"/>
                </Grid.RowDefinitions>
                <Border CornerRadius="10" Background="#f5f5f5" Grid.RowSpan="2" Grid.ColumnSpan="2">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/><!--space for button -->
                            <RowDefinition Height="1*"/><!--space for button  -->
                            <RowDefinition Height="1*"/><!--space for button  -->
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="225*"/>
                                <ColumnDefinition Width="430*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="btn_ExternalData" Style="{StaticResource Button}" Height="40" Width="205"  Grid.Column="0" Click="btn_ExternalData_Click">
                                <StackPanel Orientation="Horizontal" Width="190">
                                    <Image Source="../img/externalData.png" Width="15" Height="15" Margin="10,0,5,0"/>
                                    <TextBlock VerticalAlignment="Center" FontSize="12" Text="{x:Static local:LocalResource.BTN_EXTERNAL_DATA}"/>
                                </StackPanel>
                            </Button>
                            <TextBlock Grid.Column="1"  Height="40" TextWrapping="WrapWithOverflow" Margin="5" Text="{x:Static local:LocalResource.LBL_EXTERNAL_DATA}"></TextBlock>
                        </Grid>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="225*"/>
                                <ColumnDefinition Width="430*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="btn_CreateNewQCFile" Style="{StaticResource Button}" Height="40" Width="205" Grid.Column="0" Click="btn_CreateNewQCFile_Click" IsEnabled="False">
                                <StackPanel Orientation="Horizontal" Width="190">
                                    <Image Source="../img/CreateNew.png" Height="16" Width="14" Margin="10,0,5,0" />
                                    <TextBlock VerticalAlignment="Center" FontSize="12" Text="{x:Static local:LocalResource.BTN_CREATE_QC4}"/>
                                </StackPanel>
                            </Button>
                            <TextBlock x:Name="Qc4Text" Grid.Column="1"  TextWrapping="WrapWithOverflow" Height="40" Margin="5" Text="{x:Static local:LocalResource.LBL_CREATE_QC4}"></TextBlock>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            <Grid Grid.Row="2" Margin="24,0">
                <Button x:Name="btn_Close" Style="{StaticResource Button}" Grid.Column="4" Height="30" Width="90" HorizontalAlignment="Right" Content="{x:Static local:LocalResource.BTN_CLOSE}" Click="btn_Close_Click" Background="#aeaeae"></Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
