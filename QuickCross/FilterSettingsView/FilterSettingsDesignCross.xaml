<UserControl x:Class="FilterSettingsView.FilterSettingsDesignCross"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FilterSettingsView"
             xmlns:res="clr-namespace:FilterSettingsView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="RadioButton">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#999999"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>

        <Grid Width="620">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Border Margin="10 10 10 0" BorderThickness="1" BorderBrush="LightGray" Width="620" HorizontalAlignment="Left">
                    <Grid Margin="10 0 0 0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Style="{StaticResource TxtblockformatWithoutBg}" Text="{x:Static local:LocalResource.LABEL_CRITERION_VARIABLE}" Grid.Column="0" Margin="0,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <TextBlock Style="{StaticResource TxtblockformatWithoutBg}" Text="{x:Static local:LocalResource.LABEL_ANSWERTYPE}" Grid.Column="1" Margin="16,10,7,0" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        <TextBox Style="{StaticResource NormalTextBox_READONLY}" Name="TBAnsType" IsReadOnly="True" Grid.Column="2" VerticalContentAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,10,0,0" Width="110" Height="25" IsEnabled="False" />
                        <TextBlock  Style="{StaticResource TxtblockformatWithoutBg}" Text="{x:Static res:LocalResource.LABEL_CHOICE_COUNT}" Grid.Column="3" Margin="10,10,0,0" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        <Grid Grid.Column="4">
                            <TextBox Style="{StaticResource NormalTextBox_READONLY}"  Name="TBNoOfChoice" IsReadOnly="True" Height="25"  VerticalContentAlignment="Center" Width="100" Margin="6,10,10,0" IsEnabled="False"/>
                        </Grid>
                        <ComboBox VerticalContentAlignment="Center" Style="{StaticResource NormalComboformat}"  ItemsSource="{Binding}"  Height="25" Grid.Column="0" Grid.Row="1" Margin="0,10,100,0" Loaded="Combo_Classify_Item_Loaded"  
                                  PreviewKeyUp="KeyPress" PreviewKeyDown="Combo_Conditional_Item_1_PreviewKeyDown"
                                  TextSearch.TextPath="QuestionVariable"     IsTextSearchEnabled="True" IsEditable="True"
                                VerticalAlignment="Top"  Name="Combo_Classify_Item"   SelectionChanged="OnSelectionChanged"                          
                                HorizontalAlignment="Left"  IsEnabled="True" GotKeyboardFocus="Combo_Conditional_Item_1_GotFocus"           
                                IsSynchronizedWithCurrentItem="True" DropDownClosed="Combo_Classify_Item_DropDownClosed"
                                Width="150" ScrollViewer.HorizontalScrollBarVisibility="Visible" 
                                 >
                            <ComboBox.Resources>
                                <Style TargetType="Popup">
                                    <Setter Property="MaxWidth" Value="508"/>
                                </Style>
                            </ComboBox.Resources>
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Margin="2" Text="{Binding QuestionVariable}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="{x:Type ComboBoxItem}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate>
                                                <Grid x:Name="gd" TextElement.Foreground="Black">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="150"/>
                                                        <ColumnDefinition Width="80"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Width="150" Margin="5" Grid.Column="0" Text="{Binding QuestionVariable}"/>
                                                    <TextBlock Width="80" Margin="5" Grid.Column="1" Text="{Binding QuestionVariableType}"/>
                                                    <TextBlock Width="Auto" Margin="5" Grid.Column="2" Text="{Binding Question}"/>
                                                </Grid>

                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="ComboBoxItem.IsSelected" Value="True">
                                                        <Setter TargetName="gd"  Property="Background" Value="Gray"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>
                                                    <!--<Trigger Property="ComboBoxItem.IsKeyboardFocused"  Value="True">
                                                        <Setter TargetName="gd" Property="Background" Value="Gray"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>-->
                                                    <Trigger Property="ComboBoxItem.IsMouseOver" Value="True">
                                                        <Setter TargetName="gd"  Property="Background" Value="steelblue"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>


                                                </ControlTemplate.Triggers>

                                            </ControlTemplate>

                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>
                        <TextBlock Style="{StaticResource TxtblockformatWithoutBg}" Text="{x:Static res:LocalResource.LABEL_QUESTION}" Grid.Column="1" Grid.Row="1" Margin="18,10,0,0" Height="20" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                        <Grid Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="3">
                            <!--<TextBox Style="{StaticResource NormalTxtBox}" Grid.Column="2" DockPanel.Dock="Right" Name="TAQuestion" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"   Height="80"  IsEnabled="False" Margin="0 5 0 0"/>-->
                            <TextBox VerticalContentAlignment="Top" Padding="6 2 2 2"  Style="{StaticResource NormalTextBox_READONLY}" Grid.Column="2"  Name="TAQuestion" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" HorizontalScrollBarVisibility="Disabled"  VerticalScrollBarVisibility="Auto" Margin="0,5,10,0"  Height="80"  IsEnabled="False"/>
                        </Grid>
                        <Grid Grid.Row="2" Grid.ColumnSpan="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90*"></ColumnDefinition>
                                <ColumnDefinition Width="10*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Style="{StaticResource TxtblockformatWithoutBg}"  Text="{x:Static res:LocalResource.LABEL_EXPORT_PATH}" Grid.Column="0" Margin="0,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Height="20" Width="75"/>
                            <TextBox Style="{StaticResource NormalTextBox_READONLY}" Name="Combo_Classify_FolderPath" BorderThickness="2" Grid.Row="1" IsEnabled="False"   VerticalAlignment="Center" DockPanel.Dock="Left" Height="25" Margin="0 5 0 10"/>
                            <Button Name="exportpathbtn" Margin="0,5,0,10" Content="..." HorizontalContentAlignment="Center"  Style="{StaticResource SmallButton}"  VerticalContentAlignment="Center"  HorizontalAlignment="Right" Grid.Column="1" Grid.Row="1" Height="20" Width="30" Click="OnClick"/>

                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            <Grid Grid.Row="1">
                <CheckBox Style="{StaticResource CheckBoxCommon}" Name="Check_Refine_Condition" Checked="OnCheck" Unchecked="OnCheck"  Content="{x:Static res:LocalResource.LABEL_FILTERING_CRITERIA}"  IsEnabled="True" Grid.Row="1" Margin="20"   HorizontalAlignment="Left" VerticalAlignment="Center"/>

            </Grid>
            <Grid Grid.Row="2">
                <Border Margin="10 0 10 10"  Grid.Row="2"  BorderThickness="1"    BorderBrush="LightGray" Width="620" HorizontalAlignment="Left">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="9*"/>
                            <ColumnDefinition Width="749*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="200*"/>
                            <RowDefinition Height="230*"/>
                        </Grid.RowDefinitions>

                        <!--COLUMN NAME-->
                        <Grid Grid.Row="0" Margin="11,0,20,0" Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{StaticResource TxtblockformatWithoutBg}" Name="lblCriteriaVariable" Text="{x:Static res:LocalResource.LABEL_CRITERIA_VARIABLE}" IsEnabled="False" Grid.Column="0"  Margin="0,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <TextBlock  Style="{StaticResource TxtblockformatWithoutBg}" Name="lblOperator" Text="{x:Static res:LocalResource.LABEL_OPERATOR}" Grid.Column="1" IsEnabled="False"  Margin="15,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <TextBlock  Style="{StaticResource TxtblockformatWithoutBg}" Name="lblValue" Text="{x:Static res:LocalResource.LABEL_VALUE}" Grid.Column="2" IsEnabled="False"  Margin="50,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Grid>

                        <!--ROW 1 RULES-->
                        <Grid Grid.Row="1" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Style="{StaticResource NormalComboformat}" Height="25" Grid.Column="0" Margin="10,5,5,5"  IsEditable="True" DropDownClosed="Combo_Classify_Item_DropDownClosed"
                                  TextSearch.TextPath="QuestionVariable"     IsTextSearchEnabled="True" Loaded="Combo_Classify_Item_Loaded"
                                  PreviewKeyUp="KeyPress" PreviewKeyDown="Combo_Conditional_Item_1_PreviewKeyDown" GotFocus="Combo_Conditional_Item_1_GotFocus"
                                 ItemsSource="{Binding}" Name="Combo_Conditional_Item_1" IsEnabled="False"  GotKeyboardFocus="Combo_Classify_Item_GotKeyboardFocus"          
                                IsSynchronizedWithCurrentItem="True"  SelectionChanged="OnSelectionChanged" 
                                HorizontalAlignment="Left" Width="150" VerticalAlignment="Center" VerticalContentAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                 >
                                <ComboBox.Resources>
                                    <Style TargetType="Popup">
                                        <Setter Property="MaxWidth" Value="508"/>
                                    </Style>
                                </ComboBox.Resources>

                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="2" Text="{Binding QuestionVariable}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>

                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="{x:Type ComboBoxItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Grid x:Name="gd" TextElement.Foreground="Black">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Width="150" Margin="5" Grid.Column="0" Text="{Binding QuestionVariable}"/>
                                                        <TextBlock Width="80" Margin="5" Grid.Column="1" Text="{Binding QuestionVariableType}"/>
                                                        <TextBlock Width="Auto" Margin="5" Grid.Column="2" Text="{Binding Question}"/>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="ComboBoxItem.IsSelected" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="Gray"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>
                                                        <!--<Trigger Property="ComboBoxItem.IsKeyboardFocused"  Value="True">
                                                        <Setter TargetName="gd" Property="Background" Value="Gray"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>-->
                                                        <Trigger Property="ComboBoxItem.IsMouseOver" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="steelblue"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>


                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>





                            <ComboBox Style="{StaticResource NormalComboBox}"  Height="25" Grid.Column="1" Margin="20,5,5,5" x:Name="Combo_Conditional_Operator_1"  SelectionChanged="OnSelectionChanged"   IsEnabled="False"  VerticalAlignment="Center" HorizontalAlignment="Left" Width="150">
                                <!--<ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_EQUALS}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_OR_GREATER}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN_OR_EQUAL_TO}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN_OR_EQUAL_TO}"/>-->
                            </ComboBox>
                            <TextBox Style="{StaticResource NormalTxtBox}" Height="25" Grid.Column="2" Name="Combo_Conditional_Value_1" TextChanged="OnTextBoxChange"  HorizontalAlignment="Left" TextWrapping="Wrap" Text="" VerticalAlignment="Center" Width="103" Margin="45,5,5,5" VerticalContentAlignment="Center" IsEnabled="False"/>
                            <Button Style="{StaticResource SmallButton}" Grid.Column="2" Name="BTnFilter1"  Click="OnClick" IsEnabled="False" Width="20" Content="..." Margin="120,0,10,0"   Height="20" VerticalAlignment="Center" HorizontalAlignment="Right" />
                        </Grid>

                        <!-- ROW 2 ANDOR-->
                        <Grid Grid.Row="2" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="127*"/>
                                <ColumnDefinition Width="424*"/>
                                <ColumnDefinition Width="84*"/>
                            </Grid.ColumnDefinitions>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_AND}" Name="Option_Conditional_And_1" Checked="OnRadioClick" IsEnabled="False" Grid.Column="0" Margin="20,0,0,0"   VerticalAlignment="Center" HorizontalAlignment="Left" Height="15"></RadioButton>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_OR}" Name="Option_Conditional_Or_1" Checked="OnRadioClick" IsEnabled="False" Grid.Column="1" Margin="20,0,0,0"   Height="15" VerticalAlignment="Center" HorizontalAlignment="Left"></RadioButton>
                        </Grid>

                        <!-- ROW 3 RULES-->
                        <Grid Grid.Row="3" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Style="{StaticResource NormalComboformat}" Height="25" Grid.Column="0" Margin="10,5,5,5" PreviewKeyUp="KeyPress"   DropDownClosed="Combo_Classify_Item_DropDownClosed"
                                  TextSearch.TextPath="QuestionVariable"     IsTextSearchEnabled="True" IsEditable="True"
                                   PreviewKeyDown="Combo_Conditional_Item_1_PreviewKeyDown" GotFocus="Combo_Conditional_Item_1_GotFocus"
                                 ItemsSource="{Binding}" Name="Combo_Conditional_Item_2" IsEnabled="False"  Loaded="Combo_Classify_Item_Loaded"
                                IsSynchronizedWithCurrentItem="True" SelectionChanged="OnSelectionChanged" GotKeyboardFocus="Combo_Classify_Item_GotKeyboardFocus"
                                HorizontalAlignment="Left" Width="150"   VerticalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                 >
                                <ComboBox.Resources>
                                    <Style TargetType="Popup">
                                        <Setter Property="MaxWidth" Value="508"/>
                                    </Style>
                                </ComboBox.Resources>

                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="2" Text="{Binding QuestionVariable}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>

                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="{x:Type ComboBoxItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Grid x:Name="gd" TextElement.Foreground="Black">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Width="150" Margin="5" Grid.Column="0" Text="{Binding QuestionVariable}"/>
                                                        <TextBlock Width="80" Margin="5" Grid.Column="1" Text="{Binding QuestionVariableType}"/>
                                                        <TextBlock Width="Auto" Margin="5" Grid.Column="2" Text="{Binding Question}"/>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="ComboBoxItem.IsSelected" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="Gray"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>
                                                        <!--<Trigger Property="ComboBoxItem.IsKeyboardFocused"  Value="True">
                                                        <Setter TargetName="gd" Property="Background" Value="Gray"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>-->
                                                        <Trigger Property="ComboBoxItem.IsMouseOver" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="steelblue"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>


                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>

                            <ComboBox Style="{StaticResource NormalComboBox}"  Grid.Column="1" Height="25" Margin="20,5,5,5" Name="Combo_Conditional_Operator_2" SelectionChanged="OnSelectionChanged" IsEnabled="False"   HorizontalAlignment="Left" VerticalAlignment="Center" Width="150">
                                <!--<ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_EQUALS}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_OR_GREATER}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN_OR_EQUAL_TO}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN_OR_EQUAL_TO}"/>-->
                            </ComboBox>

                            <TextBox Style="{StaticResource NormalTxtBox}" Height="25" Grid.Column="2" Name="Combo_Conditional_Value_2" TextChanged="OnTextBoxChange" HorizontalAlignment="Left" TextWrapping="Wrap" Text="" VerticalAlignment="Center" Width="100" Margin="45,5,5,5" VerticalContentAlignment="Center" IsEnabled="False"/>
                            <Button Style="{StaticResource SmallButton}" Grid.Column="2" Name="BTnFilter2" Click="OnClick" IsEnabled="False" Width="20" Content="..." Margin="120,0,10,0"   Height="20" VerticalAlignment="Center" HorizontalAlignment="Right" />
                        </Grid>

                        <!-- ROW 4 ANDOR-->
                        <Grid Grid.Row="4" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20*"/>
                                <ColumnDefinition Width="80*"/>
                            </Grid.ColumnDefinitions>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_AND}" Name="Option_Conditional_And_2" Checked="OnRadioClick" IsEnabled="False" Grid.Column="0" Margin="20,0,0,0"  VerticalAlignment="Center" HorizontalAlignment="Left" Height="15"></RadioButton>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_OR}" Name="Option_Conditional_Or_2" Checked="OnRadioClick" IsEnabled="False" Grid.Column="2" Margin="20,0,0,0"  Height="15" VerticalAlignment="Center" HorizontalAlignment="Left"></RadioButton>
                        </Grid>

                        <!-- ROW 5 RULES-->
                        <Grid Grid.Row="5" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Style="{StaticResource NormalComboformat}" Height="25" Grid.Column="0" Margin="10,5,5,5"  PreviewKeyUp="KeyPress"  DropDownClosed="Combo_Classify_Item_DropDownClosed"
                                   PreviewKeyDown="Combo_Conditional_Item_1_PreviewKeyDown" GotFocus="Combo_Conditional_Item_1_GotFocus"
                                  TextSearch.TextPath="QuestionVariable"     IsTextSearchEnabled="True" IsEditable="True"
                                 ItemsSource="{Binding}" Name="Combo_Conditional_Item_3" IsEnabled="False"   Loaded="Combo_Classify_Item_Loaded"
                                IsSynchronizedWithCurrentItem="True" SelectionChanged="OnSelectionChanged" GotKeyboardFocus="Combo_Classify_Item_GotKeyboardFocus"
                                HorizontalAlignment="Left" Width="150"   VerticalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                 >
                                <ComboBox.Resources>
                                    <Style TargetType="Popup">
                                        <Setter Property="MaxWidth" Value="508"/>
                                    </Style>
                                </ComboBox.Resources>

                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="2" Text="{Binding QuestionVariable}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>

                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="{x:Type ComboBoxItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Grid x:Name="gd" TextElement.Foreground="Black">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Width="150" Margin="5" Grid.Column="0" Text="{Binding QuestionVariable}"/>
                                                        <TextBlock Width="80" Margin="5" Grid.Column="1" Text="{Binding QuestionVariableType}"/>
                                                        <TextBlock Width="Auto" Margin="5" Grid.Column="2" Text="{Binding Question}"/>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="ComboBoxItem.IsSelected" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="Gray"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>
                                                        <!--<Trigger Property="ComboBoxItem.IsKeyboardFocused"  Value="True">
                                                        <Setter TargetName="gd" Property="Background" Value="Gray"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>-->
                                                        <Trigger Property="ComboBoxItem.IsMouseOver" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="steelblue"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>


                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>

                            <ComboBox  Grid.Column="1" Height="25" Margin="20,5,5,5" Name="Combo_Conditional_Operator_3" SelectionChanged="OnSelectionChanged" IsEnabled="False"   HorizontalAlignment="Left" VerticalAlignment="Center" Width="150">
                                <!--<ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_EQUALS}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_OR_GREATER}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN_OR_EQUAL_TO}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN_OR_EQUAL_TO}"/>-->
                            </ComboBox>

                            <TextBox Style="{StaticResource NormalTxtBox}" Height="25" Grid.Column="2" Name="Combo_Conditional_Value_3" TextChanged="OnTextBoxChange" HorizontalAlignment="Left" TextWrapping="Wrap" Text="" VerticalAlignment="Center" Width="100" Margin="45,5,5,5" VerticalContentAlignment="Center" IsEnabled="False"/>
                            <Button Style="{StaticResource SmallButton}" Grid.Column="2" Name="BTnFilter3" Click="OnClick" IsEnabled="False" Width="20" Content="..." Margin="120,0,10,0"   Height="20" VerticalAlignment="Center" HorizontalAlignment="Right" />
                        </Grid>

                        <!-- ROW 6 ANDOR-->
                        <Grid Grid.Row="6" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20*"/>
                                <ColumnDefinition Width="80*"/>
                            </Grid.ColumnDefinitions>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_AND}" Name="Option_Conditional_And_3" Checked="OnRadioClick" IsEnabled="False" Grid.Column="0" Margin="20,0,0,0"  VerticalAlignment="Center" HorizontalAlignment="Left" Height="15"></RadioButton>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_OR}" Name="Option_Conditional_Or_3" Checked="OnRadioClick" IsEnabled="False" Grid.Column="1" Margin="20,0,0,0"   Height="15" VerticalAlignment="Center" HorizontalAlignment="Left"></RadioButton>
                        </Grid>

                        <!-- ROW 7 RULES-->
                        <Grid Grid.Row="7" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Style="{StaticResource NormalComboformat}"  Height="25" Grid.Column="0" Margin="10,5,5,5"   PreviewKeyUp="KeyPress"  DropDownClosed="Combo_Classify_Item_DropDownClosed"
                                   PreviewKeyDown="Combo_Conditional_Item_1_PreviewKeyDown" GotFocus="Combo_Conditional_Item_1_GotFocus"
                                  TextSearch.TextPath="QuestionVariable"     IsTextSearchEnabled="True" IsEditable="True"
                                 ItemsSource="{Binding}" Name="Combo_Conditional_Item_4" IsEnabled="False" Loaded="Combo_Classify_Item_Loaded"
                                IsSynchronizedWithCurrentItem="True" SelectionChanged="OnSelectionChanged" GotKeyboardFocus="Combo_Classify_Item_GotKeyboardFocus"
                                HorizontalAlignment="Left" Width="150"   VerticalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                 >
                                <ComboBox.Resources>
                                    <Style TargetType="Popup">
                                        <Setter Property="MaxWidth" Value="508"/>
                                    </Style>
                                </ComboBox.Resources>

                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="2" Text="{Binding QuestionVariable}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>

                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="{x:Type ComboBoxItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Grid x:Name="gd" TextElement.Foreground="Black">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Width="150" Margin="5" Grid.Column="0" Text="{Binding QuestionVariable}"/>
                                                        <TextBlock Width="80" Margin="5" Grid.Column="1" Text="{Binding QuestionVariableType}"/>
                                                        <TextBlock Width="Auto" Margin="5" Grid.Column="2" Text="{Binding Question}"/>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="ComboBoxItem.IsSelected" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="Gray"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>
                                                        <!--<Trigger Property="ComboBoxItem.IsKeyboardFocused"  Value="True">
                                                        <Setter TargetName="gd" Property="Background" Value="Gray"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>-->
                                                        <Trigger Property="ComboBoxItem.IsMouseOver" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="steelblue"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>


                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>

                            <ComboBox  Grid.Column="1" Margin="20,5,5,5" Height="25" Name="Combo_Conditional_Operator_4" SelectionChanged="OnSelectionChanged" IsEnabled="False"  HorizontalAlignment="Left" VerticalAlignment="Center" Width="150">
                                <!--<ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_EQUALS}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_OR_GREATER}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN_OR_EQUAL_TO}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN_OR_EQUAL_TO}"/>-->
                            </ComboBox>

                            <TextBox Style="{StaticResource NormalTxtBox}" Height="25" Grid.Column="2" Name="Combo_Conditional_Value_4" TextChanged="OnTextBoxChange" HorizontalAlignment="Left" TextWrapping="Wrap" Text="" VerticalAlignment="Center" Width="100" Margin="45,5,5,5" VerticalContentAlignment="Center" IsEnabled="False"/>
                            <Button Style="{StaticResource SmallButton}" Grid.Column="2" Name="BTnFilter4" Click="OnClick" IsEnabled="False" Width="20" Content="..." Margin="120,0,10,0"   Height="20" VerticalAlignment="Center" HorizontalAlignment="Right" />
                        </Grid>

                        <!-- ROW 8 ANDOR-->
                        <Grid Grid.Row="8" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20*"/>
                                <ColumnDefinition Width="80*"/>
                            </Grid.ColumnDefinitions>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_AND}" Name="Option_Conditional_And_4" Checked="OnRadioClick" IsEnabled="False" Grid.Column="0" Margin="20,0,0,0"   VerticalAlignment="Center" HorizontalAlignment="Left" Height="15"></RadioButton>
                            <RadioButton Style="{StaticResource Radibuttoncommon}" Content="{x:Static res:LocalResource.LABEL_OR}" Name="Option_Conditional_Or_4" Checked="OnRadioClick" IsEnabled="False" Grid.Column="1" Margin="20,0,0,0"   Height="15" VerticalAlignment="Center" HorizontalAlignment="Left"></RadioButton>
                        </Grid>

                        <!-- ROW 9 RULES-->
                        <Grid Grid.Row="9" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60*"/>
                                <ColumnDefinition Width="189*"/>
                                <ColumnDefinition Width="250*"/>
                                <ColumnDefinition Width="249*"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Style="{StaticResource NormalComboformat}" Height="25" Grid.Column="0" Margin="10,5,5,10" PreviewKeyUp="KeyPress" DropDownClosed="Combo_Classify_Item_DropDownClosed"
                                   PreviewKeyDown="Combo_Conditional_Item_1_PreviewKeyDown" GotFocus="Combo_Conditional_Item_1_GotFocus"
                                  TextSearch.TextPath="QuestionVariable"     IsTextSearchEnabled="True" IsEditable="True"
                                 ItemsSource="{Binding}" Name="Combo_Conditional_Item_5" IsEnabled="False" Loaded="Combo_Classify_Item_Loaded"
                                IsSynchronizedWithCurrentItem="True" SelectionChanged="OnSelectionChanged" GotKeyboardFocus="Combo_Classify_Item_GotKeyboardFocus"
                                HorizontalAlignment="Left" Width="150" VerticalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Visible" Grid.ColumnSpan="2"
                                 >
                                <ComboBox.Resources>
                                    <Style TargetType="Popup">
                                        <Setter Property="MaxWidth" Value="508"/>
                                    </Style>
                                </ComboBox.Resources>

                                <ComboBox.ItemTemplate >
                                    <DataTemplate >
                                        <TextBlock Margin="2" Text="{Binding QuestionVariable}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>

                                <ComboBox.ItemContainerStyle >
                                    <Style TargetType="{x:Type ComboBoxItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Grid x:Name="gd" TextElement.Foreground="Black">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="150"/>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Width="150" Margin="5" Grid.Column="0" Text="{Binding QuestionVariable}"/>
                                                        <TextBlock Width="80" Margin="5" Grid.Column="1" Text="{Binding QuestionVariableType}"/>
                                                        <TextBlock Width="Auto" Margin="5" Grid.Column="2" Text="{Binding Question}"/>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="ComboBoxItem.IsSelected" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="Gray"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>
                                                        <!--<Trigger Property="ComboBoxItem.IsKeyboardFocused"  Value="True">
                                                        <Setter TargetName="gd" Property="Background" Value="Gray"></Setter>
                                                        <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                    </Trigger>-->
                                                        <Trigger Property="ComboBoxItem.IsMouseOver" Value="True">
                                                            <Setter TargetName="gd"  Property="Background" Value="steelblue"></Setter>
                                                            <Setter TargetName="gd"  Property="TextElement.Foreground" Value="White"></Setter>
                                                        </Trigger>


                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>

                            <ComboBox  Grid.Column="2" Height="25" Margin="20,5,5,10" Name="Combo_Conditional_Operator_5" SelectionChanged="OnSelectionChanged" IsEnabled="False"   HorizontalAlignment="Left" VerticalAlignment="Center" Width="150">
                                <!--<ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_EQUALS}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_OR_GREATER}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_LESS_THAN_OR_EQUAL_TO}"/>
                                    <ComboBoxItem Content="{x:Static res:LocalResource.GTT_FS_CB_ITEM_VALUE_GREATER_THAN_OR_EQUAL_TO}"/>-->
                            </ComboBox>

                            <TextBox Style="{StaticResource NormalTxtBox}" Height="25" Grid.Column="3" Name="Combo_Conditional_Value_5" HorizontalAlignment="Left" TextWrapping="Wrap" Text="" VerticalAlignment="Center" Width="100" Margin="45,5,5,10" VerticalContentAlignment="Center" IsEnabled="False"/>
                            <Button Grid.Column="3" Name="BTnFilter5" Click="OnClick" IsEnabled="False" Width="20" Content="..." Margin="120,0,10,10"   Height="20" VerticalAlignment="Center" HorizontalAlignment="Right" />
                        </Grid>

                    </Grid>

                </Border>

            </Grid>
        </Grid>


    </Grid>
</UserControl>
